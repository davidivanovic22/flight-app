import { TranslateDurationPipe } from './trnaslate-duration.pipe';

describe('TranslateDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
